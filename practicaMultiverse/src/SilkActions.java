public interface SilkActions {
    //Lo que hace Silk (abilities)
    void handToHand (Screen s);
    void superStrength (Screen s);
    void superSpeed (Screen s);
    void reflexes (Screen s);
    void shootSpiderWeb (Screen s);
}
